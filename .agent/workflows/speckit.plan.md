---
description: Create a technical implementation plan using speckit
---

To create an implementation plan:

1. Ensure a `spec.md` exists and is current.
2. Run `/.specify/scripts/bash/setup-plan.sh`.
3. Read the generated `plan.md` template.
4. Fill in the technical details based on the spec, following `courses/speckit/plan` guidelines.
