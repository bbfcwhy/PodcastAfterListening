---
description: Generate actionable tasks from the plan using speckit
---

To generate tasks:

1. Ensure a `plan.md` exists and is complete.
2. Run `/.specify/scripts/bash/setup-tasks.sh` (if available) or create `tasks.md` from template.
3. Break down the plan into specific tasks in `tasks.md`.
