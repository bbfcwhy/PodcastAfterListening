/**
 * Admin list pagination (spec FR-011, contracts).
 */
export const DEFAULT_PAGE_SIZE = 20;
export const MAX_PAGE_SIZE = 100;

/** Max length for long text fields (ai_summary, reflection) in admin forms. */
export const EPISODE_LONG_TEXT_MAX_LENGTH = 50000;
