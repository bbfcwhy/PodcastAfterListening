# Tasks: Library 頁面「我的留言」Tab

**Input**: Design documents from `/specs/009-library-commented-tab/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/

**Tests**: 未明確要求，不包含測試任務。

**Organization**: 本功能僅有 1 個 User Story，任務按實作順序排列。

## Format: `[ID] [P?] [Story] Description`

- **[P]**: 可與其他任務平行執行（不同檔案、無相依性）
- **[Story]**: 對應的 User Story（US1）
- 所有路徑皆為相對於專案根目錄

---

## Phase 1: Setup

**Purpose**: 無需額外 Setup，本功能不新增資料表或依賴套件

（跳過 — 所有必要的依賴和基礎設施已存在）

---

## Phase 2: User Story 1 — Library 頁面「我的留言」Tab (Priority: P1) 🎯 MVP

**Goal**: 已登入使用者在 Library 頁面可以看到「我的留言」Tab，顯示自己留言過的單集列表

**Independent Test**: 登入 → 到任一單集留言 → 前往 Library 頁面 → 點擊「我的留言」Tab → 確認該單集出現並顯示留言數量

### Implementation for User Story 1

- [X] T001 [P] [US1] 建立 `src/components/library/CommentedEpisodeList.tsx`：顯示使用者留言過的單集列表，每個項目包含單集標題、所屬頻道名稱與封面圖、發佈日期、留言數量（例如「3 則留言」）。按最新留言時間倒序。空狀態顯示「還沒有留言過的單集」並附探索連結。複用 `EpisodeLibraryList.tsx` 的設計模式。
- [X] T002 [US1] 修改 `src/app/(public)/library/page.tsx`：新增查詢使用者的 approved 留言（從 `comments` 表），在應用層按 `episode_id` 分組計算留言數和最新留言時間，再查詢對應的單集詳情（`episodes` VIEW join `shows`），過濾已刪除的單集
- [X] T003 [US1] 修改 `src/app/(public)/library/page.tsx`：將 `TabsList` 的 `grid-cols-2` 改為 `grid-cols-3`，新增「我的留言」Tab，渲染 `CommentedEpisodeList` 元件

**Checkpoint**: Library 頁面完整呈現三個 Tab（頻道、單集、我的留言），可分類切換

---

## Phase 3: Polish & Cross-Cutting Concerns

**Purpose**: 驗證與收尾

- [X] T004 執行 `npm run lint` 確認無 lint 錯誤
- [ ] T005 手動驗證完整流程：登入 → 留言 → Library 頁面「我的留言」Tab → 確認顯示正確 → 確認空狀態 → 確認已刪除單集不顯示

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: 跳過（無需額外設置）
- **US1 (Phase 2)**: 可立即開始
- **Polish (Phase 3)**: 依賴 Phase 2 完成

### Within User Story 1

- T001（元件）和 T002（查詢邏輯）可平行開發
- T003（整合 Tab）依賴 T001 和 T002 完成

### Parallel Opportunities

- T001 可與 T002 平行（不同檔案）

---

## Implementation Strategy

### MVP（直接完成所有任務）

1. 平行執行 T001（元件）和 T002（查詢邏輯）
2. 完成 T003（整合到 Tabs）
3. T004 Lint 檢查
4. T005 手動驗證

---

## Notes

- 本功能範圍極小（1 個新檔案 + 1 個修改檔案），建議一次性完成
- 不需要新增資料表或 Server Actions
- 查詢使用現有的 `comments` 表，在應用層分組
- 每完成一個 Task 或邏輯群組後 commit
